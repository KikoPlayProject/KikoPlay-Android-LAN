<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kikyou.kikoplay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KikoPlayApp"
        android:allowBackup="true"
        android:icon="@drawable/kikoplays"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayActivity"
            android:theme="@style/AppFullScreenTheme"
            android:configChanges="screenSize|keyboardHidden|orientation|screenLayout"></activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|keyboardHidden|orientation|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>